{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"gh-pages\" ]; then echo \"Skipping Vercel build for gh-pages branch\"; exit 0; else exit 1; fi"
}
