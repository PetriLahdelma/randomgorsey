---
phase: 09-section-experiences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/motion/section-variants.ts
  - src/lib/motion/index.ts
autonomous: true

must_haves:
  truths:
    - "Each section has a unique motion timing profile"
    - "Section variants maintain cohesion through shared spring physics"
    - "Developer can import section-specific variants from @/lib/motion"
  artifacts:
    - path: "src/lib/motion/section-variants.ts"
      provides: "Section-specific motion variants for all 6 pages"
      exports: ["heroVariants", "galleryVariants", "listenVariants", "aboutVariants", "contactVariants", "discographyVariants"]
    - path: "src/lib/motion/index.ts"
      provides: "Barrel export including section variants"
      contains: "export * from './section-variants'"
  key_links:
    - from: "src/lib/motion/section-variants.ts"
      to: "src/lib/motion/config.ts"
      via: "springs import"
      pattern: "import.*springs.*from.*config"
---

<objective>
Create section-specific motion variants that give each page a distinct personality while maintaining cohesion.

Purpose: Enable per-section motion timing that transforms pages from "functional" to "intentionally designed" with unique character per section.
Output: `src/lib/motion/section-variants.ts` with 6 named variant exports, integrated into motion barrel export.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-section-experiences/09-RESEARCH.md
@src/lib/motion/config.ts
@src/lib/motion/variants.ts
@src/lib/motion/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create section-variants.ts with per-section motion timing</name>
  <files>src/lib/motion/section-variants.ts</files>
  <action>
Create `src/lib/motion/section-variants.ts` with the following section variants:

1. **heroVariants** - Bold entrance, dramatic timing
   - initial: opacity 0, y 40
   - enter: spring with `springs.dramatic` + 0.2s delay
   - exit: quick fade 0.2s

2. **galleryVariants** - Light, airy, graceful
   - initial: opacity 0, y 20
   - enter: spring with `springs.gentle`
   - exit: quick fade

3. **listenVariants** - Slow, deliberate, immersive
   - initial: opacity 0, y 30
   - enter: tween with duration 0.6s, ease out
   - exit: 0.3s fade

4. **aboutVariants** - Warm, natural, reading-paced
   - initial: opacity 0, y 25
   - enter: spring with custom (stiffness 70, damping 18, mass 1)
   - exit: 0.2s fade

5. **contactVariants** - Crisp, professional, efficient
   - initial: opacity 0, y 15
   - enter: spring with `springs.snappy` but lower stiffness (200)
   - exit: quick 0.15s fade

6. **discographyVariants** - Catalog reveal style
   - initial: opacity 0, y 20
   - enter: spring with `springs.default`
   - exit: 0.2s fade

Also export stagger variants per section:
- **heroStaggerContainer** - 0.1s stagger, 0.2s delay
- **galleryStaggerContainer** - 0.08s stagger, 0.1s delay
- **discographyStaggerContainer** - 0.06s stagger, 0.1s delay

Import `springs`, `durations`, `eases` from './config'.
Use `Variants` type from 'framer-motion'.
Add JSDoc comments explaining each section's character.
  </action>
  <verify>TypeScript compiles: `npm run ts:check:types` passes with no errors in section-variants.ts</verify>
  <done>Section variants file exists with 6 page variants and 3 stagger containers, all typed correctly</done>
</task>

<task type="auto">
  <name>Task 2: Update motion barrel export to include section variants</name>
  <files>src/lib/motion/index.ts</files>
  <action>
Add export for section variants to `src/lib/motion/index.ts`:

```typescript
// Section-specific variants
export * from './section-variants';
```

Place after existing variant exports.
Ensure all 6 page variants and 3 stagger containers are accessible via `@/lib/motion`.
  </action>
  <verify>Import test: Create a temp file that imports from @/lib/motion and verify heroVariants, galleryVariants etc are available</verify>
  <done>All section variants importable from @/lib/motion</done>
</task>

<task type="auto">
  <name>Task 3: Verify build and exports</name>
  <files></files>
  <action>
Run full build verification:
1. `npm run ts:check:types` - TypeScript compilation
2. `npm run build` - Production build succeeds
3. Verify exports by checking that the following import statement would work:
   `import { heroVariants, galleryVariants, listenVariants, aboutVariants, contactVariants, discographyVariants } from '@/lib/motion'`
  </action>
  <verify>`npm run build` completes without errors</verify>
  <done>Build succeeds, all section variants exported and importable</done>
</task>

</tasks>

<verification>
- [ ] `src/lib/motion/section-variants.ts` exists with 6 page variants
- [ ] Each variant has initial/enter/exit states
- [ ] Section variants use springs from config (not hardcoded)
- [ ] Barrel export includes section variants
- [ ] TypeScript compilation passes
- [ ] Production build succeeds
</verification>

<success_criteria>
Developer can `import { heroVariants } from '@/lib/motion'` and apply it to a page with section-specific motion timing. Each section variant has distinct personality through timing and physics parameters while maintaining cohesion through shared spring presets.
</success_criteria>

<output>
After completion, create `.planning/phases/09-section-experiences/09-01-SUMMARY.md`
</output>
