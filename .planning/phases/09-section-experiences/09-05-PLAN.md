---
phase: 09-section-experiences
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/pages/About.tsx
  - public/images/about-poster.jpg
autonomous: true

must_haves:
  truths:
    - "About page feels warm, personal, and storytelling-focused"
    - "Content reveals at reading pace"
    - "Portrait is a visual anchor"
    - "Side project cards have hover interaction"
  artifacts:
    - path: "src/pages/About.tsx"
      provides: "Enhanced About page with warm, personal personality"
      contains: "aboutVariants"
    - path: "public/images/about-poster.jpg"
      provides: "Poster fallback for VideoBackground"
  key_links:
    - from: "src/pages/About.tsx"
      to: "@/components/effects/VideoBackground"
      via: "component import"
      pattern: "import.*VideoBackground.*from"
    - from: "src/pages/About.tsx"
      to: "@/lib/motion"
      via: "aboutVariants import"
      pattern: "import.*aboutVariants.*from"
---

<objective>
Transform About page into a warm, personal storytelling experience with reading-paced reveals and inviting card interactions.

Purpose: About is the personal connection page - it should feel warm, unhurried, like sitting down for a conversation. The bio text should flow naturally, and side projects should feel like personal recommendations.
Output: Enhanced About.tsx using VideoBackground, aboutVariants, stagger for cards, and Container/Stack layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-section-experiences/09-RESEARCH.md
@src/pages/About.tsx
@src/components/effects/VideoBackground.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract poster frame from about video</name>
  <files>public/images/about-poster.jpg</files>
  <action>
Extract a poster frame from the about video for mobile/low-power fallback.

Option A (if ffmpeg available):
```bash
ffmpeg -i src/videos/promo_canvas.webm -vframes 1 -q:v 2 public/images/about-poster.jpg
```

Option B (fallback):
Create or use a warm-toned image. About's section tokens use warm, inviting colors (warm off-white bg, orange accent).

Requirements:
- At least 1920x1080
- Compressed to ~100-200KB
- Warm aesthetic matching the promo video
  </action>
  <verify>File exists at public/images/about-poster.jpg</verify>
  <done>Poster image ready for About VideoBackground</done>
</task>

<task type="auto">
  <name>Task 2: Enhance About.tsx with VideoBackground, aboutVariants, and improved card interactions</name>
  <files>src/pages/About.tsx</files>
  <action>
Refactor About.tsx:

1. **Replace inline video** with VideoBackground:
```tsx
import { VideoBackground } from '@/components/effects';

<VideoBackground
  src={promoCanvasVideo}
  poster="/images/about-poster.jpg"
  overlayOpacity={0.2}  // Light overlay to maintain warmth
/>
```
Remove inline `<video>` and `isWebMSupported()` check.

2. **Use aboutVariants for page transition**:
```tsx
import { motion, aboutVariants, staggerContainer, staggerItem } from '@/lib/motion';

<motion.div
  className={styles['about-container']}
  data-section="about"
  variants={aboutVariants}
  initial="initial"
  animate="enter"
  exit="exit"
>
```

3. **Add KineticText for headline**:
```tsx
import { KineticText } from '@/components/KineticText';

<RevealOnScroll>
  <KineticText as="h1" splitBy="words" variant="default">
    About
  </KineticText>
</RevealOnScroll>
```

4. **Use Container and Stack for layout**:
```tsx
import { Container } from '@/components/layout/Container';
import { Stack } from '@/components/layout/Stack';

<Container size="prose" padding="md">
  <Stack gap="xl">
    {/* Heading */}
    {/* Portrait */}
    {/* Bio */}
    {/* Side Projects */}
  </Stack>
</Container>
```
Using "prose" size for optimal reading width.

5. **Enhance portrait figure**:
```tsx
<RevealOnScroll>
  <figure className="flex justify-center my-8">
    {isWebMSupported() ? (
      <video
        title="Portrait of Random Gorsey"
        autoPlay
        muted
        loop
        controls={false}
        className="w-24 md:w-32 rounded-full shadow-xl"
        onLoadedData={handleContentLoad}
      >
        <source src="/images/portrait.webm" type="video/webm" />
        <img src="/images/portrait.jpg" alt="Portrait of Random Gorsey" className="w-24 md:w-32 rounded-full" />
      </video>
    ) : (
      <img
        src="/images/portrait.jpg"
        alt="Portrait of Random Gorsey"
        className="w-24 md:w-32 rounded-full shadow-xl"
        onLoad={handleContentLoad}
      />
    )}
  </figure>
</RevealOnScroll>
```
Replace inline styles with Tailwind classes.

6. **Enhance side project cards with stagger and hover**:
```tsx
<RevealOnScroll>
  <h2 className="text-2xl font-bold mb-4">Side projects</h2>
  <Text className={styles['about-description']}>
    Random Gorsey is also involved in several side projects, including:
  </Text>
</RevealOnScroll>

<motion.ul
  variants={staggerContainer}
  initial="initial"
  animate="enter"
  className="space-y-4 mt-6"
>
  <motion.li
    variants={staggerItem}
    className="group cursor-pointer"
    onClick={() => window.open('https://soundcloud.com/petri-lahdelma', '_blank')}
  >
    <div className="flex items-center gap-4 p-4 rounded-lg bg-white/5 hover:bg-white/10 transition-colors">
      <img
        src="/images/scportrait.jpg"
        alt="Petri Lahdelma portrait"
        className="w-16 h-16 rounded-full object-cover"
        onLoad={handleContentLoad}
      />
      <a
        href="https://soundcloud.com/petri-lahdelma"
        target="_blank"
        rel="noopener noreferrer"
        className="text-inherit no-underline group-hover:text-accent transition-colors"
      >
        Petri Lahdelma - A raw techno project on Soundcloud.
      </a>
    </div>
  </motion.li>
  {/* Second card similar structure */}
</motion.ul>
```
Remove individual RevealOnScroll from cards since stagger handles animation.

7. **Keep existing functionality**:
- Loading state with Spinner
- Portrait video/image fallback
- Click behavior on cards
  </action>
  <verify>
1. `npm run ts:check:types` passes
2. `npm run build` succeeds
3. Manual verification: About feels warm, cards stagger and have hover states
  </verify>
  <done>
- VideoBackground replaces inline video
- aboutVariants used for natural pace transition
- KineticText animates headline
- Container (prose width) provides reading-optimized layout
- Side project cards stagger in with hover interactions
- Inline styles replaced with Tailwind
  </done>
</task>

</tasks>

<verification>
- [ ] About uses VideoBackground (not inline video)
- [ ] Poster exists and is used as fallback
- [ ] aboutVariants applied (warm, natural pace)
- [ ] KineticText animates headline
- [ ] Container with prose size optimizes reading
- [ ] Side project cards stagger in
- [ ] Cards have hover interaction
- [ ] Portrait styled with Tailwind (not inline styles)
- [ ] TypeScript compiles
- [ ] Build succeeds
</verification>

<success_criteria>
About page feels distinctly different - warmer, more personal, storytelling-focused. Content reveals at a comfortable reading pace. The portrait serves as a visual anchor. Side project cards feel like personal recommendations with inviting hover states.
</success_criteria>

<output>
After completion, create `.planning/phases/09-section-experiences/09-05-SUMMARY.md`
</output>
